import { d3Color } from 'cats-client/helpers/d3-color';
import { module, test } from 'qunit';

module('Unit | Helper | d3 color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Color([42]);
  assert.ok(result);
});
